// for drag and drop
const direction_constraint_horizontal = Symbol('←→');
const direction_constraint_vertical   = Symbol('↓↑');

//for positions
const position_top = Symbol('↑');
const position_bottom = Symbol('↓');
const position_left = Symbol('←');
const position_right = Symbol('→');

export {
    direction_constraint_horizontal,
    direction_constraint_vertical,
    position_top,
    position_right,
    position_bottom,
    position_left
}
